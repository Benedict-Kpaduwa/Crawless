import React from 'react'

const Slider = () => {
  return (
    <div className='absolute left-[52px] top-0 bg-gray-600 h-full w-[243px]' id='homeComponent'>
      {/* {view === 'home' && <h1>Home Component</h1>}
      {view === 'projects' && <h1>Project Component</h1>}
      {view === 'store' && <h1>Store Component</h1>}
      {view === 'documentation' && <h1>Documentation Component</h1>}
      {view === 'settings' && <h1>Settings Component</h1>} */}
      <h1>Home Component</h1>
    </div>
  )
}

export default Slider
